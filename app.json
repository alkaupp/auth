{
  "addons": [
    "heroku-postgresql:standard-0"
  ],
  "environments": {
    "review": {
      "addons": [
        "heroku-postgresql:hobby-dev"
      ]
    }
  },
  "stack": "container",
  "scripts": {
    "postdeploy": "sed -i \"s/80/$PORT/g\" /etc/apache2/sites-enabled/000-default.conf /etc/apache2/ports.conf && docker-php-entrypoint apache2-foreground"
  }
}
